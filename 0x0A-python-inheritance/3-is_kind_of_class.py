#!/usr/bin/python3
"""is an instance"""


def is_kind_of_class(obj, a_class):
    """returns True if the object is an instance of, or if the object is an
      instance of a claaass; otherwise False."""
    return isinstance(obj, a_class)
